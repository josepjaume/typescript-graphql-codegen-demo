query GetCharacters($search: String!) {
  characters(filter: { name: $search }) {
    results {
      id
      name
      image
      status
      gender
      episode {
        id
        name
      }
      species
    }
  }
}
